version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_APP=run.py
      - FLASK_DEBUG=1
      - SECRET_KEY=dev_secret_key
      - SQLALCHEMY_DATABASE_URI=sqlite:///instance/inventory.db
